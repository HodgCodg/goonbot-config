---
name: fetch
description: Fetch any URL and return content as markdown using Lightpanda.
metadata:
  openclaw:
    emoji: "🌐"
---
# Fetch

```bash
python3 {baseDir}/scripts/fetch.py URL [--wait-ms N] [--strip full|links|none]
```

Returns rendered page content as markdown. Use for reading articles or web pages.
