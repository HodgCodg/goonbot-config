# 2026-03-26

## OpenClaw Context Window Status Bug

**Issue:** `/status` shows `17k/33k (52%)` context usage despite Qwen3.5-27B being configured with 100K context window in `openclaw.json`.

**Root Cause:** OpenClaw's `/status` command uses hardcoded default values instead of reading the actual model configuration from `models.providers.lmstudio.models[].contextWindow`.

**Config (correct):**
```json
"contextWindow": 100000,
```

**Workarounds:**
- Run `/context detail` to see actual run-time system prompt size
- Use `openclaw models list` to verify model configuration
- The 33K shown is a display bug only - actual limit remains 100K as configured

**Fix Required:** Patch OpenClaw source code where `/status` resolves active model's context window from config hierarchy.
