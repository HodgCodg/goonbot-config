- Created `procedures/fix-dashboard-persistence.md` to prevent "edit failed" / "changes not served" issues.
- The "GoonBot Guarantee" workflow for dashboard edits:
    1. Verify the actual target file being served (backend string vs. frontend file).
    2. Verify changes on disk via `cat`/`grep`.
    3. Force process reload via `systemctl --user restart status-dashboard.service`.
    4. End-to-end verification via `curl` + `grep` on the live endpoint.
- Fixed the Status Dashboard fleet expansion and router rename by updating both `status-dashboard.py` and `static/index.html` and restarting the service.
- Dashboard Refinement: Identified and fixing a bug in `fetch_ssh_system` (status-dashboard.py) where `uptime -p` was being parsed as a timestamp, causing SSH cards (like BeloitOrangePiZero3) to appear empty.
- Dashboard Refinement: Updating `PFSenseCard` title in `static/index.html` to "TXPFSenseRouter" to match config.
